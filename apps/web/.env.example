# Server-side development/preview proxy. No API keys belong in this frontend.
API_PROXY_TARGET=http://127.0.0.1:8000

# Set in the Vercel frontend project's environment variables for deployment.
# Use the API project's stable production origin, without /api.
# BACKEND_URL=https://your-api.vercel.app
